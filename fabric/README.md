# Fabric workspace artefacts

This directory is reserved for definitions exported or synchronized from a real Microsoft Fabric workspace.

PR 1 intentionally contains no hand-written Lakehouse, pipeline, notebook or semantic-model item definitions. Fabric-generated logical identifiers, bindings and metadata will be added only after a real workspace execution can be verified.

The local source fixture, contract and tests remain the reproducible baseline for that future comparison.
